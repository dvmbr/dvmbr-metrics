export default function RevenuePage() {
  return (
    <p className="text-sm text-foreground-secondary">
      Revenue analytics — arrives once the Customers vertical slice is done (see docs/roadmap.md).
    </p>
  );
}
